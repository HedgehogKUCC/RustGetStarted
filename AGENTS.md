## Language

- Reply in zh-TW

## Architecture Diagram Rules

- Use Mermaid by default.

### Mermaid Validation

Before outputting Mermaid diagrams:

- Verify the syntax is valid.
- Avoid unsupported constructs.
- Ensure the diagram can render without parse errors.
- If the diagram is large, divide it into multiple diagrams.
